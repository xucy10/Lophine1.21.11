- 根项目通过 `moe.luminolmc.hyacinthusweight.patcher` 插件定义上游依赖（Luminol）及多层补丁映射关系（paper-patches, luminol-patches, minecraft-patches）。
- 采用声明式补丁文件（`.patch`）与目录级补丁（`patchDir`）相结合的方式，将 API 层（lophine-api）与实现层（lophine-server）的修改逻辑从上游解耦。
- 子模块共享统一的 Java 21 工具链配置、Maven 发布仓库（MenthaMC）及代码质量规范，确保跨模块构建的一致性。