- 所有自定义事件类均遵循 Bukkit 规范，包含静态 `HandlerList` 字段及配套的 `getHandlers()` 和 `getHandlerList()` 方法。
- 事件类的构造函数均使用 `Validate.notNull` 对关键参数进行非空校验，确保事件触发时的数据完整性。
- 接口定义中广泛使用 `@Nullable` 和 `@NotNull` 注解（来自 `org.jetbrains.annotations`）以明确契约中的空值安全性。